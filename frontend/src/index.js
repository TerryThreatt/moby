
// routes
const BASE_URL = 'http://localhost:5500/';
const ALLDISCUSSIONS_URL = `${BACKEND_URL}/discussions`
const ALLCOMMENTS_URL = `${BACKEND_URL}/discussions`
const MYBOOKS_URL = `${BACKEND_URL}/users/${user_id}/books`
const MYDISCUSSIONS_URL = `${BACKEND_URL}/users/${user_id}/books/${book_id}/discussions`
const MYCOMMENTS_URL = `${BACKEND_URL}/users/${user_id}/books/${book_id}/discussions/${discussion_id}/comments`


// elements
const home = document.getElementById('logo');

document.addEventListener('DOMContentLoaded', () => {

});